# Dependências de runtime (espelho do pyproject.toml).
# Dev:  pip install -e ".[dev]"   (ou: just setup)

# --- Visão computacional ---
opencv-python>=4.10
mediapipe>=0.10.14
numpy>=1.26
# --- Atuação / hardware (serial) ---
pyserial>=3.5
pyserial-asyncio>=0.6
# --- API / web / config ---
fastapi>=0.115
uvicorn[standard]>=0.30
websockets>=12.0
pydantic>=2.7
pydantic-settings>=2.3
pyyaml>=6.0
loguru>=0.7
